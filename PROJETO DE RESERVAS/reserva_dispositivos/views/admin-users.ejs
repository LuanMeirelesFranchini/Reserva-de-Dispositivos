<!DOCTYPE html>
<html lang="pt-br" data-theme="white">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Gerir Utilizadores</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css"/>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <main class="container">
        <header class="logo-header">
            <img src="/images/logo_azul.png" alt="Logo do Colégio">
            <h1>Painel do Administrador</h1>
            <div style="margin-left: auto; text-align: right;">
                <p style="margin: 0;">Sessão iniciada como: <%= user.nome.split(' ')[0] %></p>
                <a href="/logout" role="button" class="secondary outline" style="padding: 5px 10px; margin-top: 5px;">Sair</a>
            </div>
        </header>

        <nav>
            <ul>
                <li><a href="/">Fazer Nova Reserva</a></li>
                <li><a href="/admin">Gerenciar Reservas</a></li>
                <li><strong>Gerir Utilizadores</strong></li>
                 <li><a href="/admin/history">Histórico de Reservas</a></li>
            </ul>
        </nav>

        <p>Usúarios que efetuaram login no sistema</p>
        
        <div style="overflow-x: auto;">
            <!-- MUDANÇA 1: Adicionada a classe "users-table" -->
            <table class="users-table">
              <thead>
                <tr>
                  <th class="col-nome">Nome</th>
                  <th class="col-email">E-mail</th>
                  <th class="col-papel">Função</th>
                  <th class="col-acoes">Alterar Permissão</th>
                </tr>
              </thead>
              <tbody>
                <% usuarios.forEach(usuario => { %>
                  <tr>
                    <td><%= usuario.nome %></td>
                    <td><%= usuario.email %></td>
                    <td><%= usuario.role.charAt(0).toUpperCase() + usuario.role.slice(1) %></td>
                    <td>
                      <div class="action-buttons">
                        <% if (usuario.id !== user.id) { %>
                          
                          <!-- MUDANÇA 2: Texto dos botões simplificado -->
                          <% if (usuario.role === 'professor') { %>
                            <form action="/admin/set-role/<%= usuario.id %>" method="POST"><input type="hidden" name="role" value="operacional"><button type="submit" class="secondary">Operacional</button></form>
                            <form action="/admin/set-role/<%= usuario.id %>" method="POST"><input type="hidden" name="role" value="admin"><button type="submit">Admin</button></form>
                          <% } else if (usuario.role === 'operacional') { %>
                            <form action="/admin/set-role/<%= usuario.id %>" method="POST"><input type="hidden" name="role" value="professor"><button type="submit" class="secondary">Professor</button></form>
                            <form action="/admin/set-role/<%= usuario.id %>" method="POST"><input type="hidden" name="role" value="admin"><button type="submit">Admin</button></form>
                          <% } else if (usuario.role === 'admin') { %>
                            <form action="/admin/set-role/<%= usuario.id %>" method="POST"><input type="hidden" name="role" value="professor"><button type="submit" class="secondary">Rebaixar</button></form>
                          <% } %>

                          <form action="/admin/delete/<%= usuario.id %>" method="POST" onsubmit="return confirm('Tem a certeza de que deseja excluir este utilizador?');">
                              <button type="submit" class="contrast">Excluir</button>
                          </form>

                        <% } else { %>
                          <p style="text-align: center; margin: 0;">(Administrador)</p>
                        <% } %>
                      </div>
                    </td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
        </div>
    </main>
</body>
</html>
