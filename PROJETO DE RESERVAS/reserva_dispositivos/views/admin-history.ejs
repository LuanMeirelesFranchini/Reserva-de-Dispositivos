<!DOCTYPE html>
<html lang="pt-br" data-theme="white">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Histórico de Reservas</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css"/>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <main class="container-fluid">
        <header class="logo-header">
            <img src="/images/logo_azul.png" alt="Logo do Colégio">
            <h1>Painel de Gestão</h1>
            <div style="margin-left: auto; text-align: right;">
                <p style="margin: 0;"><%= user.nome.split(' ')[0] %> (<%= user.role.charAt(0).toUpperCase() + user.role.slice(1) %>)</p>
                <a href="/logout" role="button" class="secondary outline" style="padding: 5px 10px; margin-top: 5px;">Sair</a>
            </div>
        </header>

        <!-- NAVEGAÇÃO CENTRALIZADA (PILLS) -->
        <nav class="nav-pills-container">
            <a href="/" class="nav-pill">Fazer Nova Reserva</a>
            <a href="/admin" class="nav-pill active">Gerenciar Reservas</a>
            <% if (user.role === 'admin') { %>
                <a href="/admin/users" class="nav-pill">Gerir Utilizadores</a>
            <% } %>
            <a href="/admin/history" class="nav-pill">Histórico de Reservas</a>
        </nav>

        <h2>Reservas concluídas e ativas no sistema</h2>
        
       <div class="table-container">
            <table class="striped">
                <thead>
                    <tr>
                        <th>Professor</th>
                        <th>Qtde</th>
                        <th>Carrinho</th>
                        <th>Feita em</th>
                        <th>Retirada</th>
                        <th>Devolução</th>
                        <th>Sala</th>
                        <th style="text-align: center;">Responsável</th>
                    </tr>
                </thead>
                <tbody>
                    <% 
                    // Definição da função de formatação ANTES de usar no loop
                    function formatarDataSimples(dataIso){
                        if(!dataIso) return '---';
                        const data = new Date(dataIso);
                        if (isNaN(data.getTime())) return 'Data Inválida';
                        
                        const dia = String(data.getDate()).padStart(2, '0');
                        const mes = String(data.getMonth()+1).padStart(2, '0');
                        const hora = String(data.getHours()).padStart(2, '0');
                        const minuto = String(data.getMinutes()).padStart(2, '0');
                        return `${hora}:${minuto} | ${dia}/${mes}`;
                    }
                    %>
                
                    <% if (reservas.length > 0) { %>
                        <% reservas.forEach(reserva => { %>
                            <tr>
                                <td><strong><%= reserva.nome_professor %></strong></td>
                                <td><%= reserva.quantidade %></td>
                                <td><%= reserva.nome_carrinho %></td>
                                
                                <!-- Data de Criação (Feita em) -->
                                <td style="white-space: nowrap;">
                                    <%= formatarDataSimples(reserva.data_reserva) %>
                                </td>

                                <!-- Data de Retirada -->
                                <td style="white-space: nowrap;">
                                    <%= formatarDataSimples(reserva.data_retirada) %>
                                </td>

                                <!-- Data de Devolução -->
                                <td style="white-space: nowrap;">
                                    <%= formatarDataSimples(reserva.data_devolucao) %>
                                </td>

                                <td><%= reserva.sala %></td>
                                
                                <td style="text-align: center;">
                                    <mark style="background: #e8f5e9; color: #2e7d32; font-size: 0.75rem; text-transform: uppercase; font-weight: bold;">
                                        <%= reserva.concluido_por || 'Concluída' %>
                                    </mark>
                                </td>
                            </tr>
                        <% }) %>
                    <% } else { %>
                        <tr><td colspan="8" style="text-align: center; padding: 2rem;">Nenhum registro encontrado no histórico.</td></tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </main>
</body>
</html>
