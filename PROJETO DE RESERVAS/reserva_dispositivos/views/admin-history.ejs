<!DOCTYPE html>
<html lang="pt-br" data-theme="white">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Histórico de Reservas</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css"/>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <main class="container-fluid">
        <header class="logo-header">
            <img src="/images/logo_azul.png" alt="Logo do Colégio">
            <h1>Painel de Gestão</h1>
            <div style="margin-left: auto; text-align: right;">
                <p style="margin: 0;"><%= user.nome.split(' ')[0] %> (<%= user.role.charAt(0).toUpperCase() + user.role.slice(1) %>)</p>
                <a href="/logout" role="button" class="secondary outline" style="padding: 5px 10px; margin-top: 5px;">Sair</a>
            </div>
        </header>

        <nav>
            <ul>
                <li><a href="/">Fazer Nova Reserva</a></li>
                <li><a href="/admin">Gerenciar Reservas</a></li>
                <% if (user.role === 'admin') { %>
                    <li><a href="/admin/users">Gerir Utilizadores</a></li>
                <% } %>
                <li><strong>Histórico de Reservas</strong></li>
            </ul>
        </nav>

        <p>Reservas concluídas e ativas no sistema</p>
        
        <div style="overflow-x: auto;">
            <table>
              <thead>
                <tr>
                  <th>Professor</th>
                  <th>Qtde</th>
                  <th>Carrinho</th>
                  <th>Retirada</th>
                  <th>Devolução</th>
                  <th>Sala</th>
                  <th>Status</th>
                </tr>
              </thead>
              <tbody>
                <% if (reservas.length > 0) { %>
                    <% reservas.forEach(reserva => { %>
                      <tr>
                        <td><%= reserva.nome_professor %></td>
                        <td><%= reserva.quantidade %></td>
                        <td><%= reserva.nome_carrinho %></td>
                        <td><%= new Date(reserva.data_retirada).toLocaleString('pt-BR') %></td>
                        <td><%= new Date(reserva.data_devolucao).toLocaleString('pt-BR') %></td>
                        <td><%= reserva.sala %></td>
                        <td><span style="color: #a5d6a7;"><%= reserva.status %></span></td>
                      </tr>
                    <% }) %>
                <% } else { %>
                    <tr>
                        <td colspan="7" style="text-align: center;">Nenhuma reserva no histórico.</td>
                    </tr>
                <% } %>
              </tbody>
            </table>
        </div>
    </main>
</body>
</html>
